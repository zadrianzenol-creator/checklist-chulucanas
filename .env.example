# Copia este archivo a ".env" y ajusta los valores.
# Local (sin servidor de BD) usa SQLite por defecto.
# En Render usaras la URL de una base PostgreSQL gratis (Neon / Render Postgres).

SECRET_KEY=tu-clave-secreta-muy-larga-y-aleatoria
DATABASE_URL=sqlite:///instance/checklist.db
ADMIN_PASSWORD=admin123